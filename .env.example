# Copy to .env for local runs (never commit .env). On Hugging Face, set this in
# Space Settings > Variables and secrets instead.

# OPTIONAL — enables the multilingual, farmer-friendly advisory rewrite.
# Free key from https://aistudio.google.com/app/apikey . Leave empty to run on
# the knowledge base in English only (no secret required).
GEMINI_API_KEY=
